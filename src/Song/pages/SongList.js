import { connect } from 'react-redux';

const SongList = () => {
  return (
    <>
      <section>{/* Filter Elements */}</section>
      <section>{/* All the songs */}</section>
    </>
  );
};

export default connect()(SongList);
