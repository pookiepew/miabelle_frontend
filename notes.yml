/song/request:
  -

/song/list/:
  SongList:
    - A form which filter songs before rendering
    - The list of songs:
        - Song

Form to filter songs:
  Filter:
    - Game: Select
    - Mode: Select
    - Routine: Select
    - Artist or title: Input


Twitch - Things to consider:
  - If user logs in, then closes the browser - Need a func to check JWT against backend on next visit
  - Auth should be checked often
  - Token refresh logic - when, how?